# Normalize text to LF in the repo
* text=auto eol=lf

# Scripts & configs (explicit LF)
*.py   text eol=lf
*.sh   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# Windows-only scripts (keep CRLF if you ever add them)
*.bat  text eol=crlf
*.ps1  text eol=crlf

# Binary (no EOL conversion)
*.dcd  binary
*.png  binary
*.jpg  binary
